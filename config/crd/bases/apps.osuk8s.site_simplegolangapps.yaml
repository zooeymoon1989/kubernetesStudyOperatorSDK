---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: simplegolangapps.apps.osuk8s.site
spec:
  group: apps.osuk8s.site
  names:
    kind: SimpleGolangApp
    listKind: SimpleGolangAppList
    plural: simplegolangapps
    singular: simplegolangapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SimpleGolangApp is the Schema for the simplegolangapps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SimpleGolangAppSpec defines the desired state of SimpleGolangApp.
            properties:
              image:
                description: Image is the container image to run
                type: string
              port:
                description: Port is the container port and service port
                format: int32
                type: integer
              replicas:
                description: Replicas is the desired number of pods
                format: int32
                type: integer
            type: object
          status:
            description: SimpleGolangAppStatus defines the observed state of SimpleGolangApp.
            properties:
              readyReplicas:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                format: int32
                type: integer
              serviceName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
